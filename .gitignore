# ============================================================================
# PRIVACY: personal-application content is a SEPARATE PRIVATE git repo
# (protocols/.git) sharing this working tree. It must NEVER be staged in this
# public repo. See CLAUDE.md § "Privacy posture". Do not remove this line.
# ============================================================================
protocols/

# Claude Code local/per-machine config (paths, permission allowlists)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Obsidian: entire local vault config (per-machine UI state, plugins, workspace)
.obsidian/
.obsidian-backup/

# OS junk
.DS_Store
Thumbs.db

# Editor swap/backup files
*.swp
*.swo
*~

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Private operating layer (local paper-archive tooling, leak-gate) — auto-loaded by Claude Code, never public
CLAUDE.local.md
